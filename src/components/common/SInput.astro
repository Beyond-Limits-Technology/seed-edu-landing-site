---
const { name, type, placeholder } = Astro.props;
---

<input
  required
  type={type}
  name={name}
  placeholder={placeholder}
  class="w-full h-[50px] bg-black bg-opacity-5 rounded px-4 border-none outline-none placeholder:text-base"
/>
