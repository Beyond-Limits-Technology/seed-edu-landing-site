---
const { title, info } = Astro.props;
---

<div class="max-w-sm p-6 bg-white border border-gray-200 rounded-lg shadow">
  <h5 class="mb-4 text-xl tracking-tight">{title}</h5>
  {
    info.map((i) => (
      <>
        <p class="mb-2 font-normal text-gray-600">• {i.title}</p>
        <p class="mb-3 font-normal">{i.subtext}</p>
      </>
    ))
  }
</div>
